name: apiKey

on:
  push:
    branches:
      - main
jobs:
  get-api-key:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v1
      - name: setup node
        uses: actions/setup-node@v1
      - name: Install dependencies
        run: npm install
      - name: Deploy to GitHub Pages
        uses: LEE-NS/WEEK1_team_introduce_page@main
        env:
          API_KEY: ${{secrets.API_KEY}}
